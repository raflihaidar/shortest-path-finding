{
  "version": 2,
  "builds": [{ "src": "/server/server.py", "use": "@vercel/python" }],
  "routes": [{ "src": "/(.*)", "dest": "/server/server.py" }],
  "functions": {
    "server/**": {
      "excludeFiles": "{.next,*.cache,node_modules,public,app}/**"
    }
  }
}
